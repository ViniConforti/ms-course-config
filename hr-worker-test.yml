test:
  config: My config value test profile update

spring:
    datasource:
        url: jdbc:h2:mem:testdb
        username: sa
        password:
    h2:
        console:
            enabled: true
            path: /h2-console
